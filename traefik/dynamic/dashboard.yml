http:
  routers:
    dashboard:
      rule: "Host(`localhost`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"
      entryPoints:
        - web
      service: api@internal
      middlewares:
        - auth

  middlewares:
    auth:
      basicAuth:
        users:
          - "admin:$apr1$y3q3XZHV$mLOdYQ4DdpjeN0RNQ1Hcb0"
          